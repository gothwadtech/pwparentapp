<resources>
    <!-- app_name is generated by Gradle from app.name in gradle.properties
         (see resValue in app/build.gradle.kts). Do not add it here: a duplicate
         string resource breaks the build. -->
</resources>
